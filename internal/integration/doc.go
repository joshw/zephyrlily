// Package integration holds end-to-end tests that wire the real tui UI to the
// real proxy and a fake Lily server (client -> proxy -> fake Lily), all
// in-process. It contains no non-test code.
package integration
